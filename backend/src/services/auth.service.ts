// Auth service — JWT login, token generation, token verification
export const authService = {
  // TODO: implement login(email: string, password: string): Promise<{ token: string }>
  // TODO: implement verifyToken(token: string): Promise<JwtPayload>
};
